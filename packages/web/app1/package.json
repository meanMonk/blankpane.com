{
  "name": "app1",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --port 3000",
    "build": "astro build && node scripts/fix-sitemap.mjs",
    "preview": "astro preview --port 3000",
    "typecheck": "astro check",
    "indexnow": "node scripts/indexnow.js",
    "functions:dev": "astro build && wrangler pages dev dist --compatibility-date=2026-06-10",
    "functions:test": "curl -s -X POST http://localhost:8788/api/feedback -H 'Content-Type: application/json' -d '{\"rating\":5,\"comment\":\"local test\",\"email\":\"a@b.c\",\"page\":\"/white-screen/\"}'"
  },
  "dependencies": {
    "@astrojs/sitemap": "3.2.1",
    "astro": "^4.15.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10"
  }
}
